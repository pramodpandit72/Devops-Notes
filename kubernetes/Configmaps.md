## ConfigMaps - Instead of hardcoding settings inside your app, you store them in ConfigMap and inject them into your container.

# What Can ConfigMap Store?
* Environment variables
* Config files
* Command-line arguments
* Key-value pairs