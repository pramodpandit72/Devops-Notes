# Project: CI/CD Pipeline

## Goal
Build a simple pipeline that runs tests on each push.

## Steps
1) Pick a repo and add a CI file.
2) Run tests on push.
3) Add a build step.
4) Add a deploy step (optional).

## Interview questions
- What triggers the pipeline?
- Why run tests in CI?
