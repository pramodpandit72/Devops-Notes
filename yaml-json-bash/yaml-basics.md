# YAML Basics

## Overview
YAML is a human-friendly data format used for config.

## Key ideas
- Indentation matters
- Use spaces, not tabs

## Example
```yaml
server:
  host: localhost
  port: 8080
```

## Interview questions
- What is YAML used for?
- Why is indentation important?
